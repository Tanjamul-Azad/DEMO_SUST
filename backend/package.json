{
  "name": "queuestorm-backend",
  "version": "1.0.0",
  "description": "QueueStorm — intelligent ticket triage for digital finance. API + SQLite + agents (Gemma 4 via Ollama, rules fallback).",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db.js --migrate",
    "seed": "node src/seed.js",
    "test": "node test/run.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "license": "MIT"
}
